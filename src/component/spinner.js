import React, { Component } from 'react';
import './spinner.css'; // Create and import a CSS file for spinner styles

class Spinner extends Component {
  render() {
    return (
      <div className="spinner"></div>
    );
  }
}

export default Spinner;
